# @capgo/capacitor-stripe-identity Demo

Exercise Stripe Identity verification sheets. Requires a verification session from your backend.

## Run

```bash
bun install
bun run start
```

For native testing:

```bash
bun run build
bunx cap sync
bunx cap open ios   # or android
```
